import 'package:flutter/material.dart';

class CrudUpdate extends StatelessWidget {
  const CrudUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
