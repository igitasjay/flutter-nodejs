import 'package:flutter/material.dart';

class CrudDelete extends StatelessWidget {
  const CrudDelete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
